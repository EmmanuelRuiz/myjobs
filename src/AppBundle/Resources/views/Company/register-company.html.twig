{% extends "AppBundle:Layouts:layoutprofiles.html.twig"%}
{% block content %}

    <div class="container">

        <div class="row">
            <div class="col-lg-6">
                <div class="col-lg-6">
                    <h1>Da de alta una empresa</h1>
                    <img src="{{ asset('assets/images/myjobsadvisor-registrar-empresa.png')}}" class="img-responsive img-register" alt="">
                </div>
            </div> 
            <div class="col-lg-6 box-form">
                {{form_start(form, {'action':'', 'method':'POST'})}}
                {{form_errors(form)}}
                {{form_end(form)}}
            </div>
        </div>

    </div>


{% endblock %}